# sonar-project.properties

# --- Core project info ---
sonar.projectKey=flask-banking-app
sonar.projectName=Flask Banking App
sonar.projectVersion=1.0

# --- Source configuration ---
# "." = entire repo; adjust if you want to exclude some dirs
sonar.sources=.

# Language / encoding
sonar.language=py
sonar.sourceEncoding=UTF-8
sonar.python.version=3.11

# --- SonarQube server (YOUR VM) ---
sonar.host.url=http://192.168.50.4:9000

# We will pass sonar.login via environment variable in the hook,
# so we do NOT put the token here.
